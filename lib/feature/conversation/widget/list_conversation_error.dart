import 'package:flutter/material.dart';

class ListConversationError extends StatelessWidget {
  const ListConversationError({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
